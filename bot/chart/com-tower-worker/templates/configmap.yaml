apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cw.fullname" . }}-config
  labels: {{- include "cw.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{- if ne (toString $v) "" }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  {{- end }}
