rules_version = '2';
// Emulator-only rules for load testing. Wide open — never used against a real project.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
